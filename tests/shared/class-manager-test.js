const getClassProperties = require('../../src/shared/class-manager');

describe('Class manager ', () => {

	test('should return the properties of the given class', () => {
		expect(true).toBe(true);
	});
})